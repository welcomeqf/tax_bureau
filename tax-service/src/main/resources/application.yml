server:
  port: 10001
  max-http-header-size: 102400
mybatis-plus:
  mapper-locations: classpath:com/dkm/*/dao/mapper/*.xml
spring:
  profiles:
    active: test
  application:
    name: tax-service
  jmx:
    enabled: false

app:
  workerId: 9
  dataCenterId: 1

